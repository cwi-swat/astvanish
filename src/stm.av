
function main(state, event, m) {
    match (m) {
        case machine _ end: 
            for (const s of $1) {
                console.log(s.src);
                match (s) {
                    case state _ _ end: 
                        if ($1.toString() === state.current) {
                            for (const t of $2) {
                                match (t) {
                                    case _ => _: 
                                        if ($1.toString() === event) {
                                            state.current = $2.toString();
                                        }
                                        return;
                                }
                            }
                        }
                }
            }
    }
}