function eval(exp, env) {
    console.log(exp.toString());
    console.log(exp.src);
    match (exp) {
        case _@Num : 
            return parseInt($1.toString());

        case _ + _ : 
            return eval($1, env) + eval($2, env);

        case _ * _ : 
            return eval($1, env) * eval($2, env);
    }
}