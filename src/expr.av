function main(exp, env) {
    console.log(exp.toString());
    match (exp) {
        case _@Num : 
            return $1.toInteger();

        case _ + _ : 
            return main($1, env) + main($2, env);

        case _ * _ : 
            return main($1, env) * main($2, env);
    }
}